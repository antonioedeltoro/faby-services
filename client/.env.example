# client/.env — copy to .env and fill in real values (never commit .env)

# Base URL of the API server
VITE_API_URL=http://localhost:5001

# Mapbox public token (used by the map on the Contact page)
VITE_MAPBOX_TOKEN=pk.your_mapbox_public_token_here
